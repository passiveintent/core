name: ci / remix

permissions:
  contents: read

on:
  push:
    branches: ['**']
    paths:
      - 'packages/remix/**'
      - 'packages/react/**'
      - 'packages/core/**'
      - 'tsconfig.base.json'
      - 'package.json'
      - 'package-lock.json'
  pull_request:
    paths:
      - 'packages/remix/**'
      - 'packages/react/**'
      - 'packages/core/**'
      - 'tsconfig.base.json'
      - 'package.json'
      - 'package-lock.json'

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build @passiveintent/core
        run: npm run build -w @passiveintent/core

      - name: Build @passiveintent/react
        run: npm run build -w @passiveintent/react

      - name: Type check
        run: npm run typecheck -w @passiveintent/remix

      - name: Unit tests
        run: npm run test -w @passiveintent/remix

      - name: Build
        run: npm run build -w @passiveintent/remix

      - name: Package integrity
        run: npm run verify:package -w @passiveintent/remix
