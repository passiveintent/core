{
  "name": "@passiveintent/vanilla",
  "version": "0.0.1",
  "private": true,
  "description": "Archived placeholder — Vanilla adapter exploration, not part of the active workspace or release flow.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -b",
    "build": "node -e \"console.log('TODO: implement esbuild IIFE build — see src/index.ts for details')\""
  },
  "dependencies": {
    "@passiveintent/core": "^1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3"
  }
}
