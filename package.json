{
  "name": "privacy-first-intent-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc src/intent-sdk.ts --target ES2022 --module NodeNext --moduleResolution NodeNext --outDir dist",
    "test": "npm run build && node --test tests/intent-sdk.test.mjs"
  }
}
